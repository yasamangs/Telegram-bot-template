#  ready to go template for a Telegram Bot

All the basic needs for creating a Telegram Bot.

## How to run

First add `src` to `PYTHONPATH` by type

```
export PYTHONPATH=${PWD}
```
in the terminal.

then run

```
python src/bot.py
```